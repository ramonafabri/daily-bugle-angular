server:
  port: 8080
  servlet:
    context-path: /daily-bugle

spring:
  application:
    name: daily-bugle
  datasource:
    url: jdbc:mysql://localhost:3306/daily-bugle?createDatabaseIfNotExist=true&autoReconnect=true&characterEncoding=utf8&useUnicode=true&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: test1234
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
  output:
    ansi:
      enabled: ALWAYS
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

logging:
  level:
    hu.progmasters: debug
    org.hibernate: warn
    org.hibernate.SQL: trace
    org.springframework.web: info
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss}){faint} %clr(%-5level) %clr([%t]){faint} %clr(%c{36}){blue} %m%n"